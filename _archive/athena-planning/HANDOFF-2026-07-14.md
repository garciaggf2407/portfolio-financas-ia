# Handoff — portfolio-financas-ia (2026-07-14, ~18h)

Cole isto no início da próxima sessão que for continuar este projeto.

---

## Contexto

Estou dando continuidade ao projeto **portfolio-financas-ia**
(Blueprint `BP-2026-07-13-003`, ATHENA OS), localizado em
`projects/portfolio-financas-ia` dentro do repositório `athena-os-main`.
Antes de fazer qualquer coisa, leia:

1. `projects/portfolio-financas-ia/.planning/STATE.md` — estado detalhado
2. `git -C projects/portfolio-financas-ia log --oneline -15` — fonte de
   verdade real (o STATE.md é local, não versionado, e pode ficar
   desatualizado ou conter narrativas erradas se múltiplas sessões
   escreveram nele sem coordenar — **confirme contra o git log antes de
   confiar em qualquer afirmação de "quem fez o quê" no STATE.md**)

## Estado atual (confirmado, não é suposição)

- Blueprint concluído: 6/6 épicos (E-1 a E-6), CP-1 a CP-6 PASSED
- Deploy público no ar e validado:
  - Backend: https://financas-ia-api.onrender.com (`/actuator/health`)
  - Frontend: https://portfolio-financas-ia.vercel.app
  - Postgres (Neon) + RabbitMQ (CloudAMQP) gerenciados, conectados
- **E-7 (novo epic, fora do blueprint original) — suporte a OFX**: T-7.1
  feito e pushado (`5218544`). Parser implementado contra a especificação
  pública do OFX, mas **nunca testado contra um arquivo `.ofx` real de
  banco brasileiro** — ao contrário do parser de CSV (já validado contra
  extrato real do Nubank, que expôs 1 bug real de verdade). Isso é o
  gap de maior valor a fechar antes de seguir.
- **E-8 (planejado, não iniciado) — melhorias de dashboard**: só existem
  notas de pesquisa (benchmark Monarch Money/Copilot Money/YNAB) no
  STATE.md cuja origem eu não confirmei pessoalmente — trate como
  não-verificado até revisar.

## Alerta importante: sessões concorrentes

Confirmei 3 sessões `claude.exe` simultâneas hoje (via lista de
processos): esta, uma em `plantoes-medicos-saas` e uma com MCP de Gmail.
A de `plantoes-medicos-saas` aparentemente também opera dentro deste
mesmo repositório (`athena-os-main`) e chegou a escrever no
`STATE.md` deste projeto por engano, criando uma narrativa confusa que
já foi corrigida.

**Não trabalhe em `portfolio-financas-ia` a partir de duas sessões ao
mesmo tempo.** Se outra sessão (ex. a de médicos) também tiver acesso a
este repositório, não deixe as duas mexendo em arquivos deste projeto
simultaneamente — já causou um quase-conflito real em `README.md` mais
cedo hoje. Antes de commitar/dar push, rode `git status` e `git log
origin/master..master` para confirmar que não há divergência.

## Próximos passos possíveis (escolha um)

1. Conseguir um arquivo `.ofx` real (qualquer banco) e validar o
   `OfxTransactionParser` contra ele, mesmo padrão do que foi feito com
   o CSV do Nubank.
2. Fragmentar e iniciar E-8 (dashboard): KPIs no topo, cards por métrica,
   ícones de categoria, filtros/busca em `DashboardPage.tsx`/
   `TransactionsPage.tsx`.

## Referências

- Memória (auto-memory do Claude): `project_portfolio_financas_ia_e5.md`,
  `feedback_hybrid_checkpoints_waived.md`, `user_profile_guilherme.md`
- `docs/adr/001-modelo-dados.md`, `docs/adr/002-categorizacao-assincrona.md`
- `docs/DEPLOYMENT.md` (guia completo de infra)
- `outputs/blueprints/2026-07-13/portfolio-financas-ia/` (blueprint
  original + `validation-report.yaml`)
