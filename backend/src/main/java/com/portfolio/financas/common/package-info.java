/**
 * Cross-cutting concerns shared by every feature package: the error
 * response contract from docs/openapi.yaml (ErrorResponse) and the
 * exceptions/handler that translate domain failures into HTTP responses.
 */
package com.portfolio.financas.common;
